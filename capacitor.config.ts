import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.feragostb.treelogger',
  appName: 'TreeLogger',
  webDir: 'dist/tree-logger/browser'
};

export default config;
